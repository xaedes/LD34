'use strict';

define(['phaser'], function(Phaser) {
    function TreeGenome() {
        // parameter class to hold all parameters relevant to generate a tree
    }
    TreeGenome.prototype.constructor = TreeGenome;
    return TreeGenome;
});